---
Title: WSH-0002
---

:::warning
THIS DOCUMENTATION IS STILL IN DEVELOPMENT AND SUBJECT TO CHANGE.
:::

## WSH-0002 Anomalous Payload Delivered from ISO files

## Document Classification

This document is only for internal use of Wortell Managed Detection and Response.

## Introduction

The purpose of this document is to explain why case _"WSH-0002 Anomalous Payload Delivered from ISO files"_ has been created, how to use it, how to investigate it, and what to do in case of true positive incidents.

### Required Access

These cases are linked through a published application that grants us permission to perform threat hunting via the API in our customer's security portals.

### Custom Changes

* No custom changes are made.

## Overview

This case searches for .lnk file executions from other locations than the C: drive, which can relate to mounted ISO-files.

### MITRE ATT&CK

This attack tactic can be mapped on the MITRE ATT&CK framework:

| Tactic | Techniques |
|--------|------------------|
| **Execution** | **T1204** User Execution |

## Applicability & Technical Requirements

This case is applicable to customers that subscribe to the Wortell MDR **Device Protect** module.

### How to Detect?

Verify the device activity based on the **DeviceEvents** and **DeviceProcessEvents** tables within Advanced Hunting to see if an ISO file is mounted that contains .lnk files.

### KQL

```kql
    DeviceEvents
    | where Timestamp > ago(1d)
    | where ActionType == 'BrowserLaunchedToOpenUrl' 
    | where RemoteUrl endswith ".lnk"
    | where RemoteUrl !startswith "C:"
    | project LNKLaunchTimestamp = Timestamp, DeviceName, RemoteUrl
    | parse RemoteUrl with Drive '\\' *
    | extend Drive= tostring(Drive)
    | where isnotempty(Drive)
    | join kind=innerunique (
    DeviceProcessEvents
    | where Timestamp > ago(1d)
    | where FolderPath !startswith "C:"
    | parse FolderPath with Drive '\\' *
    | project Drive= tostring(Drive), StartedProcessTimestamp = Timestamp, StartedProcessName = FileName, StartedProcessSHA1 = SHA1, StartedProcessCommandline = ProcessCommandLine, StartedProcessPath = FolderPath, DeviceName, StartedProcessParentName = InitiatingProcessFileName, StartedProcessParentCmdline = InitiatingProcessCommandLine, StartedParentProcessFolderPath = InitiatingProcessFolderPath, StartedProcessGrandParent = InitiatingProcessParentFileName, Timestamp
    ) on DeviceName, Drive
    | where StartedProcessTimestamp between (LNKLaunchTimestamp ..(LNKLaunchTimestamp+1m))
    | project-away Drive1, DeviceName1
    | project-reorder LNKLaunchTimestamp, StartedProcessTimestamp, DeviceName, RemoteUrl, Drive, StartedProcessName, StartedProcessSHA1, StartedProcessPath,StartedProcessCommandline, StartedProcessParentName, StartedProcessParentCmdline, StartedParentProcessFolderPath, StartedProcessGrandParent, Timestamp
```

## How to Investigate?

To investigate this case, investigate:

* The file hash
* What device is being used?
* What folder is it initiating from?

Copy the hash from the alert and paste it in [VirusTotal](https://www.virustotal.com/). Check if the hash of the file was flagged. If it's not flagged, check the context in which the file is placed.

## How to respond?

If the incident is identified as true positive, proceed with standard remediation actions

### Custom Remediation Actions

* This case does not have any custom remediation actions.

### Standard Remediation Actions

The following standard remediation actions have to be executed in order to contain the threat:

* [ws-vrt-communicate-customer](../../../Usecases/remediation-actions/ws-vrt-communicate-customer.md)
