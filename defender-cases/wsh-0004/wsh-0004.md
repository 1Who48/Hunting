---
Title: WSH-0004
---

:::warning
THIS DOCUMENTATION IS STILL IN DEVELOPMENT AND SUBJECT TO CHANGE.
:::

## WSH-0004 Detecting EDR-killing Tool

## Document Classification

This document is only for internal use of Wortell Managed Detection and Response.

## Introduction

The purpose of this document is to explain why usecase _"WSH-0004 Detecting EDR-killing Tool"_ has been created, how to use it.

### Required Access

These hunting cases are linked through a published application that grants us permission to perform threat hunting via the API in our customers' security portals.

### Custom Changes

* No custom changes are made.

## Overview

Recent reports indicate that a cybercrime group associated with the RansomHub ransomware has been utilizing a new tool designed to disable endpoint detection and response (EDR) software on compromised systems. This tool, known as EDRKillShifter, functions as a loader executable and serves as a delivery mechanism for a legitimate driver that is vulnerable to exploitation. This technique is commonly referred to as a ‘bring your own vulnerable driver’ (BYOVD) attack.

The following KQL script is an enhancement of my previous threat hunting efforts to detect BYOVD scenarios. It is important to note that threat actors may modify your driver system files for malicious purposes. This modification can trigger low prevalence detections. If further analysis confirms that the driver is being loaded and tampered with, it is highly likely that an EDR-Killing tool is present in your environment.

### MITRE ATT&CK

This attack tactic can be mapped on the MITRE ATT&CK framework:

| Tactic                | Techniques |
|-----------------------|------------|
|  Defense Evasion      | T1036      |

## Applicability & Technical Requirements

This use case is applicable to customers that subscribe to the Wortell MDR **Device Protect** module.

### How to Detect?

The **"DeviceFileEvents"** we can hunt on strange activity based on file creation, Drive load event and Tampering attempt detection.

### KQL

```kql
    let DriverwithLowPrevalence =
    DeviceFileEvents
    | where Timestamp > ago(1d)
    | where ActionType == "FileCreated"
    | where FileName endswith ".sys"
    | invoke FileProfile(SHA1,10000)
    | where GlobalPrevalence <= 150
    | invoke FileProfile(SHA1,1000)
    | where GlobalPrevalence <= 150 or isempty(GlobalPrevalence)
    | join kind=leftouter DeviceFileCertificateInfo on SHA1
    | project FileName;
    let DevicewithLPDriverActivated =
    DeviceEvents
    // Event ID 3004 — Kernel-mode Driver Validation
    | where ReportId == "3004"
    | where ActionType == @"DriverLoad"
    | where FileName has_any(DriverwithLowPrevalence)
    | distinct DeviceName;
    DeviceEvents
    | where ReportId == "5013"
    | where ActionType == @"TamperingAttempt"
    | where DeviceName has_any(DevicewithLPDriverActivated)
    | distinct DeviceName,ActionType,InitiatingProcessFileName,AdditionalFields
```

## How to Investigate?

1. Validate the driver’s legitimacy
    * Identify the loaded .sys file and determine whether it belongs to a known vendor, internal tool, or recently installed software.
    * Review digital signature status, certificate trust chain, and signing timestamp to spot stolen, expired, or test certificates.

2. Assess driver risk and intent
    * Low-prevalence drivers are commonly used for BYOVD (Bring Your Own Vulnerable Driver) or custom rootkits—research whether the driver is known to disable security controls or grant kernel access.
    * Check whether the driver version is outdated or associated with publicly documented vulnerabilities.

3. Investigate the driver load sequence
    * Determine how the driver was introduced (installer, script, dropper, living-off-the-land binary).
    * Correlate driver load time with user activity, process execution, software installs, or privilege escalation attempts.

4. Analyze tampering attempts
    * Review the tampering event details to understand which security control was targeted (EDR, Defender, kernel protections).
    * Pay close attention to the initiating process—unexpected binaries, renamed tools, or unsigned executables are strong indicators of malicious intent.

5. Evaluate device and account exposure
    * Confirm whether the affected device is used by a privileged user or has access to sensitive systems.
    * Look for signs of persistence such as services, scheduled tasks, boot-start drivers, or registry modifications.

## How to respond?

If the incident is identified as true positive, proceed with standard remediation actions

### Custom Remediation Actions

* This case does not have any custom remediation actions.

### Standard Remediation Actions

The following standard remediation actions have to be executed in order to contain the threat:

* [ws-vrt-contain-device](../../../Usecases/remediation-actions/ws-vrt-contain-device.md)
* [ws-vrt-communicate-customer](../../../Usecases/remediation-actions/ws-vrt-communicate-customer.md)
