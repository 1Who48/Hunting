---
Title: WSH-0403
---

:::warning
THIS DOCUMENTATION IS STILL IN DEVELOPMENT AND SUBJECT TO CHANGE.
:::

## WSH-0403 Port Opened For Azure Resource

## Document Classification

This document is only for internal use of Wortell Managed Detection and Response.

## Introduction

The purpose of this document is to explain why case _"WSH-0403 Port Opened For Azure Resource"_ has been created, how to use it, how to investigate it, and what to do in case of true positive incidents.

### Required MDR Modules and Sentinel Connectors

| MDR Module                       | Required Data Connectors            |
|--------------------------------- |------------------------------------ |
| **Landingzone Protect**          | Azure Security Center               |
|                                  | Azure Activity                      |
|                                  | Azure Key Vault                     |
|                                  | Azure Storage Account               |
|                                  | DNS                                 |
|                                  | Security Events                     |

### Custom Changes

* No custom changes have been made

## Overview

Identifies what ports may have been opened for a given Azure Resource over the last 7 days

### MITRE ATT&CK

This attack tactic can be mapped on the MITRE ATT&CK framework:

| Tactic                | Techniques |
|-----------------------|------------|
| Command And Control   | T1071      |
| Impact                | T1571      |
|                       | T1496      |

## Applicability & Technical Requirements

This use case is applicable to customers that subscribe to the Wortell MDR **"Landingzone Protect"**

### How to detect?

The **"AzureActivity"** logs of the customer need to be exported to the Wortell MDR Sentinel Log Analytics Workspace with the Microsoft Sentinel **"Azure Activity"** connector of Sentinel.

### KQL

```kql
    AzureActivity
    | where TimeGenerated > ago(1d)
    | where OperationNameValue has_any ("ipfilterrules", "securityRules", "publicIPAddresses", "firewallrules") and OperationNameValue endswith "write"
    // Choosing Accepted here because it has the Rule Attributes included
    | where ActivityStatusValue == "Accepted" 
    // If there is publicIP info, include it
    | extend parsed_properties = parse_json(tostring(parse_json(Properties).responseBody)).properties
    | extend publicIPAddressVersion = case(Properties has_cs 'publicIPAddressVersion', tostring(parsed_properties.publicIPAddressVersion), "")
    | extend publicIPAllocationMethod = case(Properties has_cs 'publicIPAllocationMethod', tostring(parsed_properties.publicIPAllocationMethod), "")
    // Include rule attributes for context
    | extend access = case(Properties has_cs 'access', tostring(parsed_properties.access), "")
    | extend description = case(Properties has_cs 'description', tostring(parsed_properties.description), "")
    | extend destinationPortRange = case(Properties has_cs 'destinationPortRange', tostring(parsed_properties.destinationPortRange), "")
    | extend direction = case(Properties has_cs 'direction', tostring(parsed_properties.direction), "")
    | extend protocol = case(Properties has_cs 'protocol', tostring(parsed_properties.protocol), "")
    | extend sourcePortRange = case(Properties has_cs 'sourcePortRange', tostring(parsed_properties.sourcePortRange), "")
    | summarize StartTime = min(TimeGenerated),EndTime = max(TimeGenerated),ResourceIds = make_set(_ResourceId, 100) by Caller,CallerIpAddress,Resource,ResourceGroup,ActivityStatusValue,ActivitySubstatus,SubscriptionId,access,description,destinationPortRange,direction,protocol,sourcePortRange,publicIPAddressVersion,publicIPAllocationMethod
    | extend Name = tostring(split(Caller, '@', 0)[0]),UPNSuffix = tostring(split(Caller, '@', 1)[0])
    | extend Account_0_Name = Name
    | extend Account_0_UPNSuffix = UPNSuffix
    | extend IP_0_Address = CallerIpAddress
```

## How to investigate?

Open ports can expose services to external threats if not properly configured.

* Identify the open ports associated with the Azure resource.
* Verify the session of the administrator or service principal that performed the action.
* Ensure there are no overly permissive rules (e.g., "any-any").

### Custom Investigation

* This hunt case does not have any custom investigation actions

### Standard Investigation Queries

* This hunt case does not have any standard investigation actions

## How to respond?

If the incident is identified as true positive, proceed with standard remediation actions

### Custom Remediation Actions

* This case does not have any custom remediation actions.

### Standard Remediation Actions

If the account which performed this action shows any signs of compromise the following standard remediation actions have to be executed in order to contain the threat:

* [ws-vrt-contain-account](../../../Usecases/remediation-actions/ws-vrt-contain-account.md)
* [ws-vrt-communicate-customer](../../../Usecases/remediation-actions/ws-vrt-communicate-customer.md)

If there are no signs of active compromises then the following standard remediation actions have to be executed to inform the customer about this open port:

* [ws-vrt-communicate-customer](../../../Usecases/remediation-actions/ws-vrt-communicate-customer.md)
